#include <bits/stdc++.h>
using namespace std;
const int size = 100000;
#define rep(i, a, b) for (int i = (int)a; i < (int)b; i++)
int main()
{
    int n, m;
    cin >> n >> m;
    int a[n];
    
    return 0;
}